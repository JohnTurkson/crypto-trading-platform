import UserCredentials from "../data/UserCredentials"

export default async function getUserCredentials(id: string): Promise<UserCredentials> {
    // TODO
    return {
        id: "",
        password: "",
    }
}
